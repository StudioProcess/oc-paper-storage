{
  "name": "oc-paper-storage",
  "version": "0.0.1",
  "description": "",
  "repository": "github:StudioProcess/oc-paper-storage",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.6.0"
  },
  "scripts": {
    "start": "browser-sync . --port 8080 --no-notify",
    "test": "echo \"Error: no test specified\" && exit 1",
    "reinstall": "rm -rf node_modules && npm install"
  },
  "keywords": [],
  "author": "Process Studio <contact@process.studio> (https://process.studio)",
  "license": "AGPL-3.0",
  "dependencies": {
    "three": "^0.95.0",
    "vue": "^2.5.17"
  },
  "devDependencies": {
    "browser-sync": "^2.24.6"
  }
}
